
from collections import defaultdict
def is_anagram(str, str2) -> bool:
    if len(str) == len(str2):
        default_dict = defaultdict(int)
        for char in str:
            default_dict[chr] += 1



    return True